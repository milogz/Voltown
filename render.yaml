services:
  - type: web
    name: voltown-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Inicio.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
